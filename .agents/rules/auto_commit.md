# Auto Commit Rule

**Description**: Automatically commit verified changes without asking for user approval.

When completing a set of bug fixes or feature changes that successfully pass verification tests (or when I am confident they are correct), I MUST automatically stage and commit the changes using `git add` and `git commit` with a descriptive commit message. Do NOT ask for the user's approval before committing, just do it automatically.
